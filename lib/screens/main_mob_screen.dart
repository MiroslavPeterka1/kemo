import 'package:flutter/material.dart';

class MainMobScreen extends StatelessWidget {
  const MainMobScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Mob screen'),
      ),
    );
  }
}
